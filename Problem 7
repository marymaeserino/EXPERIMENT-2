#include<iostream>
using namespace std;

int main()
{
	int num, sum=0;
	
	do{
		cout<<"Enter a number: ";
		cin>>num;
		for(int i=0; i<=num; i++){
			sum+=i;
		}
		cout<<"The sum of whole numbers from 1 to "<<num<<" is "<<sum<<endl;
		sum=0;
	}while(num>0);
	system("PAUSE");
	return 0;
}
